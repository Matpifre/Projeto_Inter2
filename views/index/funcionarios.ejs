<h1>Pagina de produtos !!</h1>

<p>Listagem de funcionarios (
    <%= produtos.length %> funcionario disponível)</p>

<% for (let contador = 0; contador < produtos.length; contador++) { %>

    <h2>
        <%= produtos[contador].nome %>
    </h2>

    <p>
        Id:
        <%= produtos[contador].id %>
    </p>

    <p>
        Valor: R$
        <%= produtos[contador].valor %>
    </p>

    <hr />

    <% } %>